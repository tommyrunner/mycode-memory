#!/usr/bin/env node

const fs = require("fs");
const path = require("path");

const agentsFile = path.join(process.cwd(), "AGENTS.md");

const template = `# MyCode Memory

## 文档规则

- 本文件用于记忆用户在本项目中的个人编码习惯，不是常规编码规则或通用最佳实践文档。
- 只记录用户明确表达过的习惯、Codex 在对话中反馈过并形成结论的项目事实，以及当前项目中能直接验证的稳定信息。
- 项目代码相关任务应先读取本文件，再开始编码。
- 普通写代码、改代码、修 bug、重构、阅读项目时只读取并遵循本文件，不自动更新本文件。
- 只有用户明确调用 mycode-memory 并要求更新或刷新记忆时，才修改本文件。
- 没有依据的章节保持“暂无记录”，不要补通用建议。
- 更新时必须先读取已有内容，再融合新信息，进行去重、合并、修正或替换。
- 当用户调用 mycode-memory 要求更新或刷新记忆时，默认需要主动回顾当前完整对话上下文，不只看最后一条消息。
- 不要把新信息无意义地一直追加到末尾。
- 不使用“当前模块”“当前页面”“当前这次观察”等临时上下文表述。
- 全文使用中文记录。

## 称呼

- 当前称呼：小C。
- 当前称呼是用户为 Codex 设置的个性化称呼，不替代 Codex 的产品身份。
- 下文的“「当前称呼」”是占位说明，回复时必须替换为上方记录的实际称呼，不能原样输出。
- 称呼规则适用于所有对话，不限于代码任务。每个新对话的第一条回复都必须自然使用当前称呼，让用户能确认称呼已经生效。
- 用户问“你叫什么”“怎么称呼你”“你是谁”等身份或称呼问题时，必须优先回答“你可以叫我「当前称呼」，我是你的编程协作助手”。如需说明产品身份，可以补充“我是 Codex”，但不得只回答“我叫 Codex”。
- 开始执行任务时，必须使用当前称呼明确表明由自己执行，例如“「当前称呼」会先检查项目约束”。
- 后续沟通自然使用当前称呼，不需要在每句话中重复自称。

## 项目基本信息

### 项目概览

- 暂无记录。

### 技术栈

- 暂无记录。

### 公共能力

#### 公共组件库

- 暂无记录。

#### 公共函数与工具

- 暂无记录。

#### 公共 Hook、服务或模块

- 暂无记录。

#### 公共常量、类型、样式或配置

- 暂无记录。

### 文件结构

1. 暂无记录。

> 记录要求：使用序号明确说明项目整体结构；公共重要文件可以写具体路径和用途；业务模块只写最上层目录、主要入口或大功能，不展开所有小文件。

### 功能地图

- 暂无记录。

## 注释习惯

### 记忆

- 暂无记录。

### 示例

- 暂无记录。

## 打印与调试习惯

### 记忆

- 暂无记录。

### 示例

- 暂无记录。

## 界面组件化习惯

### 记忆

- 暂无记录。

### 示例

- 暂无记录。

> 记录重点：用户在界面绘制时希望如何拆组件、哪些区域独立为组件、组件如何分层、业务组件放在哪里，以及对话中出现过的具体实例。

## 文件结构习惯

### 记忆

- 暂无记录。

### 示例

- 暂无记录。

> 记录重点：这里记录新增或修改模块时的文件组织习惯；项目整体结构写在“项目基本信息 / 文件结构”。

## 测试与验证习惯

### 记忆

- 暂无记录。

### 示例

- 暂无记录。

> 记录重点：只记录用户明确说过的验证偏好，或对话中已经执行/反馈过的验证行为。例如用户明确说“不需要 build 打包”，就记住该习惯；如果没有相关依据，保持“暂无记录”。

## 记忆刷新规则

- 只有用户明确调用 mycode-memory 并要求刷新、细化、更新或整理记忆时，才更新本文件。
- “更新记忆”默认表示：主动回顾当前完整对话上下文，再与已有记忆融合更新。
- 刷新时只记录用户明确话术、Codex 对话反馈、项目中可验证事实。
- 局部模块信息要抽象为项目级记忆，再融合到对应章节。
- 刷新时如果用户明确要求修改 Codex 的称呼，使用当前对话中最后一次明确指定的称呼替换“称呼 / 当前称呼”，并在刷新后的回复中立即采用新称呼；没有明确修改时保留原值，缺失时使用默认称呼“小C”。
- 没有对话依据或项目证据的章节保持“暂无记录”。
- 更新不是追加清单，而是融合已有记忆后产出更准确的结果。
`;

if (fs.existsSync(agentsFile)) {
  console.log(`AGENTS.md 已存在，不会覆盖：${agentsFile}`);
  process.exit(0);
}

fs.writeFileSync(agentsFile, template, "utf8");
console.log(`已创建 MyCode 记忆：${agentsFile}`);
